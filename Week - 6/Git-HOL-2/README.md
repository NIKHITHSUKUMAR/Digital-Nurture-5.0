# Git Ignore Hands-On Lab

This exercise demonstrates how to use `.gitignore` to exclude unwanted files and folders from version control.

## Ignored Items

- All `.log` files
- `log/` directory

## Files

- `welcome.txt`
- `.gitignore`

## Outcome

Verified that ignored files and folders are not tracked by Git.